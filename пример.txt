<!DOCTYPE html>
<html lang="ru">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
<title>ColorKit — UI Reference</title>
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@fontsource/inter@5.0.18/index.min.css" />
<style>
  :root {
    --bg: #eef0f4;
    --bg-2: #e3e6ec;
    --card: #ffffff;
    --ink: #1c2230;
    --ink-soft: #5b6475;
    --accent: #2f6df6;
    --accent-soft: #dbe6ff;
    --ok: #1fa971;
    --shadow: 0 30px 60px -25px rgba(20, 30, 60, 0.25), 0 10px 25px -15px rgba(20, 30, 60, 0.15);
    --radius: 28px;
  }

  * { box-sizing: border-box; margin: 0; padding: 0; }

  html, body {
    font-family: 'Inter', system-ui, -apple-system, sans-serif;
    color: var(--ink);
    background:
      radial-gradient(1200px 600px at 10% -10%, #f6f8fc 0%, transparent 60%),
      radial-gradient(900px 500px at 110% 10%, #eaf0ff 0%, transparent 55%),
      linear-gradient(180deg, var(--bg) 0%, var(--bg-2) 100%);
    min-height: 100vh;
    -webkit-font-smoothing: antialiased;
    overflow-x: hidden;
  }

  .page {
    max-width: 1440px;
    margin: 0 auto;
    padding: 48px 32px 80px;
  }

  /* ---------- Header ---------- */
  header.hero {
    display: flex;
    align-items: flex-end;
    justify-content: space-between;
    gap: 32px;
    margin-bottom: 48px;
    flex-wrap: wrap;
  }
  .brand {
    display: flex;
    align-items: center;
    gap: 14px;
  }
  .logo {
    width: 44px; height: 44px;
    border-radius: 14px;
    background: conic-gradient(from 210deg, #ff7a59, #ffd166, #06d6a0, #4cc9f0, #7b6cf6, #ff7a59);
    box-shadow: inset 0 0 0 4px rgba(255,255,255,.85), 0 8px 20px -6px rgba(80,90,180,.4);
  }
  .brand h1 { font-size: 22px; letter-spacing: -0.02em; }
  .brand span { color: var(--ink-soft); font-size: 13px; }

  .hero-text h2 {
    font-size: clamp(28px, 3.4vw, 44px);
    line-height: 1.05;
    letter-spacing: -0.03em;
    max-width: 720px;
  }
  .hero-text p {
    margin-top: 14px;
    color: var(--ink-soft);
    max-width: 620px;
    font-size: 15px;
    line-height: 1.55;
  }
  .tag {
    display: inline-flex; align-items: center; gap: 8px;
    padding: 6px 12px; border-radius: 999px;
    background: var(--accent-soft); color: var(--accent);
    font-size: 12px; font-weight: 600; letter-spacing: .02em;
    margin-bottom: 14px;
  }
  .tag::before {
    content: ""; width: 6px; height: 6px; border-radius: 50%;
    background: var(--accent); box-shadow: 0 0 0 4px rgba(47,109,246,.18);
  }

  /* ---------- Stage with 3 screens ---------- */
  .stage {
    display: grid;
    grid-template-columns: 320px 1fr 360px;
    gap: 28px;
    align-items: start;
  }
  @media (max-width: 1180px) {
    .stage { grid-template-columns: 1fr; }
  }

  .screen {
    background: var(--card);
    border-radius: var(--radius);
    box-shadow: var(--shadow);
    overflow: hidden;
    position: relative;
  }
  .screen-label {
    position: absolute; top: 14px; left: 14px;
    font-size: 11px; font-weight: 600; letter-spacing: .08em;
    text-transform: uppercase;
    color: var(--ink-soft);
    background: rgba(255,255,255,.7);
    backdrop-filter: blur(6px);
    padding: 6px 10px; border-radius: 999px;
    z-index: 5;
  }

  /* ---------- Screen 1: Home ---------- */
  .home {
    padding: 28px 22px;
    min-height: 640px;
    display: flex; flex-direction: column;
  }
  .lang-switch {
    align-self: flex-end;
    display: inline-flex;
    background: #eef1f7;
    border-radius: 999px;
    padding: 4px;
    font-size: 12px; font-weight: 600;
  }
  .lang-switch button {
    border: 0; background: transparent; cursor: pointer;
    padding: 6px 12px; border-radius: 999px;
    color: var(--ink-soft); font: inherit;
    transition: all .2s ease;
  }
  .lang-switch button.active {
    background: #fff; color: var(--ink);
    box-shadow: 0 2px 6px rgba(20,30,60,.08);
  }

  .upload-orb {
    margin: 40px auto 28px;
    width: 220px; height: 220px;
    border-radius: 50%;
    background:
      radial-gradient(circle at 30% 25%, #cfe4ff 0%, #8bbcff 45%, #4a8bf0 100%);
    display: flex; align-items: center; justify-content: center;
    text-align: center;
    color: #0b2a5e;
    font-weight: 700; font-size: 18px; line-height: 1.25;
    box-shadow:
      inset 0 -20px 40px rgba(20,60,140,.25),
      inset 0 20px 40px rgba(255,255,255,.55),
      0 30px 50px -20px rgba(60,110,220,.55);
    cursor: pointer;
    transition: transform .25s ease;
    position: relative;
  }
  .upload-orb:hover { transform: scale(1.03); }
  .upload-orb::after {
    content: ""; position: absolute; inset: -14px;
    border-radius: 50%;
    border: 2px dashed rgba(74,139,240,.35);
    animation: spin 22s linear infinite;
  }
  @keyframes spin { to { transform: rotate(360deg); } }

  .btn-primary {
    display: block; width: 100%;
    padding: 14px 18px; border-radius: 16px;
    background: linear-gradient(180deg, #3a78f7, #2559d6);
    color: #fff; font-weight: 600; font-size: 15px;
    border: 0; cursor: pointer;
    box-shadow: 0 12px 24px -10px rgba(37,89,214,.6);
    transition: transform .15s ease;
  }
  .btn-primary:hover { transform: translateY(-1px); }

  .catalog-card {
    margin-top: 22px;
    border-radius: 20px;
    aspect-ratio: 4 / 3;
    background:
      linear-gradient(180deg, rgba(20,30,50,.15), rgba(20,30,50,.55)),
      url('data:image/svg+xml;utf8,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 400 300"><defs><linearGradient id="g" x1="0" x2="0" y2="1"><stop offset="0" stop-color="%23aab4c4"/><stop offset="1" stop-color="%23556074"/></linearGradient></defs><rect width="400" height="300" fill="url(%23g)"/><path d="M0 220 L80 160 L150 200 L230 130 L310 180 L400 140 L400 300 L0 300 Z" fill="%23394256" opacity=".7"/><path d="M0 250 L90 210 L180 240 L260 200 L340 230 L400 210 L400 300 L0 300 Z" fill="%23222a3a" opacity=".8"/></svg>');
    background-size: cover;
    display: flex; align-items: flex-end;
    padding: 18px;
    color: #fff;
    position: relative;
    overflow: hidden;
  }
  .catalog-card .badge-soon {
    position: absolute; top: 14px; right: 14px;
    background: rgba(255,255,255,.2);
    backdrop-filter: blur(6px);
    padding: 4px 10px; border-radius: 999px;
    font-size: 11px; font-weight: 600;
  }
  .catalog-card h3 { font-size: 18px; }
  .catalog-card p { font-size: 12px; opacity: .8; margin-top: 2px; }

  /* ---------- Screen 2: iPad canvas ---------- */
  .ipad-wrap {
    display: flex; justify-content: center; align-items: flex-start;
    gap: 18px;
    padding: 8px 0;
  }
  .ipad {
    width: 100%;
    max-width: 560px;
    background: #0f1115;
    border-radius: 44px;
    padding: 18px;
    box-shadow:
      0 40px 80px -30px rgba(10,20,40,.55),
      inset 0 0 0 2px #2a2e38;
    position: relative;
  }
  .ipad::before {
    content: ""; position: absolute; top: 22px; left: 50%;
    transform: translateX(-50%);
    width: 10px; height: 10px; border-radius: 50%;
    background: #1a1d24; box-shadow: inset 0 0 0 2px #2a2e38;
  }
  .ipad-screen {
    background: #fafbfd;
    border-radius: 28px;
    overflow: hidden;
    aspect-ratio: 3 / 4;
    display: flex; flex-direction: column;
    position: relative;
  }

  .toolbar {
    display: flex; align-items: center; gap: 10px;
    padding: 12px 16px;
    background: rgba(255,255,255,.85);
    backdrop-filter: blur(10px);
    border-bottom: 1px solid rgba(20,30,60,.06);
    z-index: 3;
  }
  .tool-btn {
    width: 34px; height: 34px; border-radius: 10px;
    display: inline-flex; align-items: center; justify-content: center;
    background: #f1f3f8; color: var(--ink);
    border: 0; cursor: pointer;
    transition: background .15s ease;
  }
  .tool-btn:hover { background: #e6eaf2; }
  .tool-btn svg { width: 18px; height: 18px; }
  .brush-slider {
    flex: 1;
    height: 6px; border-radius: 999px;
    background: linear-gradient(90deg, var(--accent) 0%, var(--accent) 45%, #dde2ec 45%, #dde2ec 100%);
    position: relative;
    margin: 0 6px;
  }
  .brush-slider::after {
    content: ""; position: absolute; top: 50%; left: 45%;
    transform: translate(-50%, -50%);
    width: 16px; height: 16px; border-radius: 50%;
    background: #fff; box-shadow: 0 2px 6px rgba(20,30,60,.25);
  }
  .toggle {
    width: 42px; height: 24px; border-radius: 999px;
    background: var(--accent);
    position: relative; cursor: pointer;
  }
  .toggle::after {
    content: ""; position: absolute; top: 3px; right: 3px;
    width: 18px; height: 18px; border-radius: 50%;
    background: #fff; box-shadow: 0 2px 4px rgba(0,0,0,.2);
  }

  .canvas-area {
    flex: 1;
    position: relative;
    overflow: hidden;
  }
  .canvas-area img.bg {
    position: absolute; inset: 0;
    width: 100%; height: 100%; object-fit: cover;
  }
  .numbers-overlay {
    position: absolute; inset: 0;
    pointer-events: none;
  }
  .num {
    position: absolute;
    font-size: 13px; font-weight: 700;
    color: #2a2f3a;
    text-shadow: 0 1px 2px rgba(255,255,255,.7);
    transform: translate(-50%, -50%);
  }
  .fit-btn {
    position: absolute; right: 12px; bottom: 12px;
    width: 38px; height: 38px; border-radius: 12px;
    background: rgba(255,255,255,.9);
    backdrop-filter: blur(8px);
    display: inline-flex; align-items: center; justify-content: center;
    box-shadow: 0 6px 16px rgba(20,30,60,.15);
    cursor: pointer; border: 0;
  }

  .palette-bar {
    margin: 10px 14px 14px;
    background: rgba(255,255,255,.92);
    backdrop-filter: blur(10px);
    border-radius: 999px;
    padding: 8px 10px;
    display: flex; align-items: center; gap: 8px;
    overflow-x: auto;
    box-shadow: 0 10px 24px -12px rgba(20,30,60,.25);
  }
  .palette-bar::-webkit-scrollbar { display: none; }
  .swatch {
    flex: 0 0 auto;
    width: 34px; height: 34px; border-radius: 50%;
    display: inline-flex; align-items: center; justify-content: center;
    font-size: 12px; font-weight: 700;
    cursor: pointer;
    transition: transform .15s ease;
    position: relative;
  }
  .swatch:hover { transform: scale(1.08); }
  .swatch.active {
    transform: scale(1.18);
    box-shadow: 0 0 0 3px #fff, 0 0 0 5px var(--accent);
  }
  .swatch.add {
    background: #eef1f7;
    color: var(--ink-soft);
    border: 2px dashed #c4cad6;
  }

  .pencil {
    width: 14px;
    align-self: stretch;
    min-height: 520px;
    background: linear-gradient(180deg, #f4f5f7 0%, #d9dce2 50%, #f4f5f7 100%);
    border-radius: 8px 8px 2px 2px;
    position: relative;
    box-shadow: 0 20px 40px -20px rgba(20,30,60,.4);
  }
  .pencil::before {
    content: ""; position: absolute; top: 30px; left: 0; right: 0;
    height: 18px; background: #2a2e38; border-radius: 2px;
  }
  .pencil::after {
    content: ""; position: absolute; bottom: -14px; left: 50%;
    transform: translateX(-50%);
    width: 0; height: 0;
    border-left: 7px solid transparent;
    border-right: 7px solid transparent;
    border-top: 14px solid #c9ccd3;
  }

  /* ---------- Screen 3: Processing ---------- */
  .processing {
    padding: 28px 24px;
    min-height: 640px;
    display: flex; flex-direction: column;
  }
  .proc-preview {
    margin-top: 18px;
    border-radius: 20px;
    aspect-ratio: 4 / 3;
    overflow: hidden;
    position: relative;
    background: #0e1320;
  }
  .proc-preview img {
    width: 100%; height: 100%; object-fit: cover;
    filter: saturate(.4) contrast(1.05);
  }
  .proc-preview .scan {
    position: absolute; left: 0; right: 0; height: 3px;
    background: linear-gradient(90deg, transparent, var(--accent), transparent);
    box-shadow: 0 0 18px var(--accent);
    animation: scan 2.4s ease-in-out infinite;
  }
  @keyframes scan {
    0% { top: 0%; } 50% { top: 100%; } 100% { top: 0%; }
  }
  .proc-preview .overlay-grid {
    position: absolute; inset: 0;
    background-image:
      linear-gradient(rgba(255,255,255,.08) 1px, transparent 1px),
      linear-gradient(90deg, rgba(255,255,255,.08) 1px, transparent 1px);
    background-size: 28px 28px;
    mix-blend-mode: overlay;
  }

  .proc-title {
    margin-top: 22px;
    font-size: 18px; font-weight: 700;
    letter-spacing: -0.01em;
  }
  .proc-sub { color: var(--ink-soft); font-size: 13px; margin-top: 4px; }

  .progress {
    margin-top: 18px;
    height: 8px; border-radius: 999px;
    background: #eef1f7; overflow: hidden;
  }
  .progress > div {
    height: 100%; width: 62%;
    background: linear-gradient(90deg, #4a8bf0, #7b6cf6);
    border-radius: 999px;
    animation: prog 3s ease-in-out infinite;
  }
  @keyframes prog {
    0% { width: 20%; } 50% { width: 78%; } 100% { width: 20%; }
  }
  .proc-percent {
    display: flex; justify-content: space-between;
    margin-top: 8px; font-size: 12px; color: var(--ink-soft);
  }

  .steps {
    margin-top: 22px;
    display: flex; flex-direction: column; gap: 10px;
  }
  .step {
    display: flex; align-items: center; gap: 12px;
    padding: 10px 12px;
    border-radius: 14px;
    background: #f6f8fc;
    font-size: 13px;
  }
  .step .dot {
    width: 22px; height: 22px; border-radius: 50%;
    display: inline-flex; align-items: center; justify-content: center;
    font-size: 11px; font-weight: 700; color: #fff;
    background: #c4cad6;
  }
  .step.done .dot { background: var(--ok); }
  .step.active { background: var(--accent-soft); color: var(--accent); font-weight: 600; }
  .step.active .dot { background: var(--accent); }

  .smart-note {
    margin-top: auto;
    padding: 14px;
    border-radius: 16px;
    background: linear-gradient(135deg, #eef4ff, #f3eeff);
    font-size: 12px; line-height: 1.5;
    color: var(--ink-soft);
    display: flex; gap: 10px;
  }
  .smart-note strong { color: var(--ink); display: block; margin-bottom: 2px; font-size: 13px; }
  .smart-note .spark {
    flex: 0 0 auto;
    width: 28px; height: 28px; border-radius: 10px;
    background: conic-gradient(from 0deg, #ff7a59, #ffd166, #06d6a0, #4cc9f0, #7b6cf6, #ff7a59);
  }

  /* ---------- Footer notes ---------- */
  .notes {
    margin-top: 56px;
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
    gap: 18px;
  }
  .note {
    background: rgba(255,255,255,.7);
    backdrop-filter: blur(8px);
    border: 1px solid rgba(20,30,60,.06);
    border-radius: 20px;
    padding: 20px;
  }
  .note h4 { font-size: 14px; margin-bottom: 6px; letter-spacing: -0.01em; }
  .note p { font-size: 13px; color: var(--ink-soft); line-height: 1.55; }
  .note .k {
    display: inline-block;
    font-family: ui-monospace, SFMono-Regular, Menlo, monospace;
    font-size: 11px;
    background: #eef1f7; color: var(--ink);
    padding: 2px 6px; border-radius: 6px;
    margin-right: 4px;
  }

  footer.legal {
    margin-top: 40px;
    text-align: center;
    color: var(--ink-soft);
    font-size: 12px;
  }
</style>
</head>
<body>
  <div class="page">

    <!-- HERO -->
    <header class="hero">
      <div>
        <div class="brand">
          <div class="logo"></div>
          <div>
            <h1>ColorKit</h1>
            <span>UI Reference · iPad + Apple Pencil Pro</span>
          </div>
        </div>
        <div style="margin-top: 28px;" class="hero-text">
          <span class="tag">Smart paint-by-numbers</span>
          <h2>Загрузите фото — получите готовую картину по номерам.</h2>
          <p>Алгоритм сам анализирует изображение, подбирает палитру и количество цветов так, чтобы силуэт и детали читались. Никаких настроек сложности — только результат.</p>
        </div>
      </div>
    </header>

    <!-- STAGE -->
    <section class="stage">

      <!-- SCREEN 1: HOME -->
      <div class="screen home">
        <span class="screen-label">01 · Главный экран</span>
        <div class="lang-switch">
          <button class="active">RU</button>
          <button>EN</button>
        </div>

        <div class="upload-orb">
          Загрузить<br/>фото
        </div>

        <button class="btn-primary">Мои проекты</button>

        <div class="catalog-card">
          <span class="badge-soon">Скоро</span>
          <div>
            <h3>Каталог</h3>
            <p>Готовые картины для раскрашивания</p>
          </div>
        </div>
      </div>

      <!-- SCREEN 2: iPAD CANVAS -->
      <div class="ipad-wrap">
        <div class="ipad">
          <div class="ipad-screen">
            <span class="screen-label" style="color:#fff; background: rgba(0,0,0,.35);">02 · Холст раскрашивания</span>

            <div class="toolbar">
              <button class="tool-btn" title="Отмена">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M9 14 4 9l5-5"/><path d="M4 9h11a5 5 0 0 1 0 10h-3"/></svg>
              </button>
              <button class="tool-btn" title="Повтор">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="m15 14 5-5-5-5"/><path d="M20 9H9a5 5 0 0 0 0 10h3"/></svg>
              </button>
              <div class="brush-slider"></div>
              <div class="toggle" title="Ластик"></div>
              <button class="tool-btn" title="Сохранить">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M19 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h11l5 5v11a2 2 0 0 1-2 2z"/><path d="M17 21v-8H7v8M7 3v5h8"/></svg>
              </button>
            </div>

            <div class="canvas-area">
              <img class="bg" alt="landscape" src="https://image.pollinations.ai/prompt/watercolor%20painting%20of%20mountain%20valley%20with%20winding%20river%20at%20sunset%2C%20soft%20pastel%20colors%2C%20paint-by-numbers%20style%20with%20visible%20thin%20black%20outlines%20between%20color%20regions%2C%20partially%20colored%20some%20areas%20still%20white%20paper%2C%20vertical%20composition?width=720&height=960&nologo=true&seed=42" />
              <div class="numbers-overlay">
                <span class="num" style="top:18%; left:30%;">3</span>
                <span class="num" style="top:22%; left:55%;">14</span>
                <span class="num" style="top:28%; left:72%;">22</span>
                <span class="num" style="top:38%; left:42%;">16</span>
                <span class="num" style="top:42%; left:62%;">33</span>
                <span class="num" style="top:48%; left:28%;">4</span>
                <span class="num" style="top:52%; left:50%;">9</span>
                <span class="num" style="top:58%; left:70%;">11</span>
                <span class="num" style="top:64%; left:35%;">22</span>
                <span class="num" style="top:70%; left:55%;">20</span>
                <span class="num" style="top:74%; left:75%;">7</span>
                <span class="num" style="top:80%; left:25%;">13</span>
                <span class="num" style="top:84%; left:48%;">12</span>
              </div>
              <button class="fit-btn" title="Вписать">
                <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="#1c2230" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M15 3h6v6M9 21H3v-6M21 3l-7 7M3 21l7-7"/></svg>
              </button>
            </div>

            <div class="palette-bar">
              <div class="swatch" style="background:#3a78f7; color:#fff;">5</div>
              <div class="swatch" style="background:#e8743b; color:#fff;">4</div>
              <div class="swatch" style="background:#1fa971; color:#fff;">3</div>
              <div class="swatch" style="background:#f0a020; color:#1c2230;">2</div>
              <div class="swatch" style="background:#f5d020; color:#1c2230;">1</div>
              <div class="swatch" style="background:#3aa657; color:#fff;">5</div>
              <div class="swatch" style="background:#7b4bd6; color:#fff;">6</div>
              <div class="swatch active" style="background:#1c2230; color:#fff;">8</div>
              <div class="swatch" style="background:#d63a3a; color:#fff;">7</div>
              <div class="swatch add">+</div>
            </div>
          </div>
        </div>
        <div class="pencil" aria-hidden="true"></div>
      </div>

      <!-- SCREEN 3: PROCESSING -->
      <div class="screen processing">
        <span class="screen-label">03 · Умная обработка</span>

        <div class="proc-preview">
          <img alt="preview" src="https://image.pollinations.ai/prompt/mountain%20valley%20with%20river%20at%20sunset%20photo%20realistic?width=600&height=450&nologo=true&seed=7" />
          <div class="overlay-grid"></div>
          <div class="scan"></div>
        </div>

        <div class="proc-title">Анализируем изображение…</div>
        <div class="proc-sub">Алгоритм подбирает палитру и количество цветов автоматически.</div>

        <div class="progress"><div></div></div>
        <div class="proc-percent">
          <span>Шаг 3 из 5</span>
          <span>62%</span>
        </div>

        <div class="steps">
          <div class="step done"><span class="dot">✓</span> Анализ цветов изображения</div>
          <div class="step done"><span class="dot">✓</span> Построение палитры</div>
          <div class="step active"><span class="dot">3</span> Сегментация на области</div>
          <div class="step"><span class="dot">4</span> Контуры и номера</div>
          <div class="step"><span class="dot">5</span> Финализация</div>
        </div>

        <div class="smart-note">
          <div class="spark"></div>
          <div>
            <strong>Умное форматирование</strong>
            Количество цветов и детализация подбираются под конкретное фото — силуэт и ключевые детали всегда остаются читаемыми.
          </div>
        </div>
      </div>

    </section>

    <!-- NOTES -->
    <section class="notes">
      <div class="note">
        <h4>Экран 01 · Главный</h4>
        <p>Большая круглая кнопка загрузки, быстрый доступ к проектам, заглушка каталога, переключатель языка <span class="k">RU</span><span class="k">EN</span>.</p>
      </div>
      <div class="note">
        <h4>Экран 02 · Холст</h4>
        <p>Слои: фон → контуры с номерами → краска. Номера закрашиваются как в реальной картине. Лента палитры снизу, цвет цифры — автоконтрастный.</p>
      </div>
      <div class="note">
        <h4>Экран 03 · Обработка</h4>
        <p>Единый умный pipeline без выбора сложности. Прогресс по шагам, предпросмотр исходника, поясняющая плашка о логике алгоритма.</p>
      </div>
      <div class="note">
        <h4>Палитра</h4>
        <p>Кругляшки с номерами, горизонтальная прокрутка, активный выделен обводкой. Кнопка <span class="k">+</span> — свободный цвет без номера.</p>
      </div>
    </section>

    <footer class="legal">
      Визуальный референс · текст и компоновка соответствуют спецификации проекта · финальные строки берутся из <code>locales/ru.json</code>
    </footer>

  </div>

<script>
  // Language switch demo
  document.querySelectorAll('.lang-switch button').forEach(btn => {
    btn.addEventListener('click', () => {
      btn.parentElement.querySelectorAll('button').forEach(b => b.classList.remove('active'));
      btn.classList.add('active');
    });
  });

  // Swatch selection demo
  document.querySelectorAll('.swatch:not(.add)').forEach(sw => {
    sw.addEventListener('click', () => {
      document.querySelectorAll('.swatch').forEach(s => s.classList.remove('active'));
      sw.classList.add('active');
    });
  });

  // Eraser toggle demo
  const toggle = document.querySelector('.toggle');
  toggle.addEventListener('click', () => {
    const on = toggle.style.background !== 'rgb(221, 226, 236)';
    toggle.style.background = on ? '#dde2ec' : 'var(--accent)';
    toggle.style.setProperty('--knob', on ? 'left' : 'right');
    const knob = toggle.querySelector('::after');
    toggle.style.cssText += on
      ? '; --x: 3px;'
      : '';
    // simple visual flip via pseudo not directly stylable; use class instead
    toggle.classList.toggle('off');
  });

  // Fit button pulse
  document.querySelector('.fit-btn').addEventListener('click', (e) => {
    e.currentTarget.animate(
      [{ transform: 'scale(1)' }, { transform: 'scale(0.92)' }, { transform: 'scale(1)' }],
      { duration: 220, easing: 'ease-out' }
    );
  });
</script>

<style>
  .toggle.off { background: #dde2ec !important; }
  .toggle.off::after { right: auto; left: 3px; }
</style>
</body>
</html>
